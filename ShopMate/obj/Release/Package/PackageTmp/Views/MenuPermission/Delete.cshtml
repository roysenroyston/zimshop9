@model ShopMate.Models.MenuPermission

@{
    ViewBag.Title = "Are you sure you want to delete";
    if (Ajax.IsThisAjaxRequest())
    {
        Layout = null;
    }
}


@Html.StarkAjaxFormSubmiter("frmMenuPermission", "tbMenuPermission", true, "Action Successfully Executed")

@{
    if (!Ajax.IsThisAjaxRequest())
    {
        <section class="content-header">
            <h1>
                @ViewBag.Title
                <small>@ViewBag.Title</small>
            </h1>
        </section>
    }
}

<section class="content">
    <!-- Small boxes (Stat box) -->
    <div class="row">
        <div class="col-xs-12">

            <div class="box box-solid box-primary">
                <div class="box-header">
                    <h3 class="box-title">Detail of MenuPermission</h3>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive">
                  
                    <div class="widget-sec">
                        <div class="streaming-table">
                            <table class="table table-striped table-bordered">

                                <tr>
                                    <td>MenuId</td>
                                    <td>@Html.DisplayFor(model => model.MenuId)</td>
                                </tr>
                                <tr>
                                    <td>RoleId</td>
                                    <td>@Html.DisplayFor(model => model.RoleId)</td>
                                </tr>
                                <tr>
                                    <td>UserId</td>
                                    <td>@Html.DisplayFor(model => model.UserId)</td>
                                </tr>
                                <tr>
                                    <td>SortOrder</td>
                                    <td>@Html.DisplayFor(model => model.SortOrder)</td>
                                </tr>
                                <tr>
                                    <td>IsCreate</td>
                                    <td>@Html.DisplayFor(model => model.IsCreate)</td>
                                </tr>
                                <tr>
                                    <td>IsRead</td>
                                    <td>@Html.DisplayFor(model => model.IsRead)</td>
                                </tr>
                                <tr>
                                    <td>IsUpdate</td>
                                    <td>@Html.DisplayFor(model => model.IsUpdate)</td>
                                </tr>
                                <tr>
                                    <td>IsDelete</td>
                                    <td>@Html.DisplayFor(model => model.IsDelete)</td>
                                </tr>


                            </table>
                            <div class="box-footer">

                                  @using (Html.BeginForm("Delete", "MenuPermission", FormMethod.Post, new {  id = "frmMenuPermission" }))
                                {
                                    @Html.AntiForgeryToken()

                                    @Html.StarkInputButtion("Delete","submit","btn btn-danger","")
                                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



