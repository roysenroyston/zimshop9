@model ShopMate.Models.ProductCategory
@{
    ViewBag.Title = "Product List";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.MenuOpen = "Product";
}
@section ScriptsOrCss
{
    <link href="~/Content/DataTable/new/dataTables.bootstrap.css" rel="stylesheet" type="text/css" />
    <script src="~/Content/DataTable/new/jquery.dataTables.min.js" type="text/javascript"></script>
    <script src="~/Content/DataTable/new/dataTables.buttons.min.js" type="text/javascript"></script>
    <script src="~/Content/DataTable/new/jszip.min.js" type="text/javascript"></script>
    <script src="~/Content/DataTable/new/pdfmake.min.js" type="text/javascript"></script>
    <script src="~/Content/DataTable/new/vfs_fonts.js" type="text/javascript"></script>
    <script src="~/Content/DataTable/new/buttons.html5.min.js" type="text/javascript"></script>
    <link href="~/Content/DataTable/new/jquery.dataTables.min.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/DataTable/new/buttons.dataTables.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Content/DataTable/new/buttons.print.min.js"></script>
    <script src="~/Content/DataTable/new/buttons.colVis.min.js"></script>
<link href="~/Content/Site.css" rel="stylesheet" />
}
<script src="~/Scripts/quagga.js"></script>
<section class="content-header">
    <h1>
        @ViewBag.Title
        <small> @ViewBag.Title</small>
    </h1>
</section>

<section class="content">
    <!-- Small boxes (Stat box) -->
    <div id="barcode-scanner" style="display:none;"></div>
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-solid box-primary">

                <div class="box-body">
                    <!-- Split button -->
                    <div >
                        <div class="btn-group">
                            @Ajax.ModalDialogActionLink("Create Quick", "Create", "Create ", "btn btn-dropbox")
                        </div>
                        <div class="btn-group">
                            <a href="~/Product/Create" class="btn btn-default">Create Full</a>
                        </div>
                        <div class="btn-group">
                            <button id="updateprice" class="btn btn-success" data-target="#progressmodal" data-toggle="modal" >Increase Line Product Prices</button>
                           
                        </div>
                        <div class="btn-group">
                            <button class="btn btn-danger" data-target="#dprice" data-toggle="modal" >Decrease Line Product Prices</button>
                        </div>
                        <div class="btn-group">
                            <button class="btn btn-success" data-target="#allprice" data-toggle="modal" >Increase All Product Prices</button>
                        </div>
                        <div class="btn-group">
                            <button class="btn btn-danger" data-target="#dallprice" data-toggle="modal" >Decrease all Product Prices</button>
                        </div>
                    </div>
                   
                    <!-- flat split buttons -->
                </div><!-- /.box-body -->
            </div>
            <div class="box box-solid box-primary">
                <div class="box-header">
                    <h3 class="box-title">@ViewBag.Title</h3>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive">

                    <div class="margin">

                        <div class="streaming-table">
                            <span id="found" class="label label-info"></span>


                            <table id="tbProduct" class="display nowrap">
                                <thead>
                                    <tr>
                                        <th>Actions</th>
                                        <th> Id  </th>
                                        <th> Name  </th>
                                        @*<th> Product Category  </th>*@
                                        <th> Bar Code  </th>
                                                       <th>HSN Code</th>
                                        <th> Purchase Price   </th>
                                        <th>  Sale Price  </th>
                                        @*<th> Sale Rtgs Price  </th>*@
                                        <th> Product Description  </th>
                                       @* <th> Product Image  </th>*@
                                       @* <th> Added By  </th>*@
                                        @*<th> Date Added  </th>*@
                                        @*<th> Is Active  </th>*@
                                        <th>Warehouse Id</th>
                                     @*   <th> Stock Alert  </th>*@
                                        <th> RemainingQuantity </th>
                                        <th> TaxId  </th>

                                    </tr>
                                </thead>
                                <tbody>
                                    
                                </tbody>
                            </table>

                            <div id="summary">
                                <div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="box box-solid box-primary">

                <div class="box-body">
                    <!-- Split button -->
                    <div class="margin">
                        <div class="btn-group">
                            @Ajax.ModalDialogActionLink("Create Quick", "Create", "Create ", "btn btn-dropbox")
                        </div>
                        <div class="btn-group">
                            <a href="~/Product/Create" class="btn btn-default">Create Full</a>
                        </div>
                    </div>
                    <!-- flat split buttons -->
                </div><!-- /.box-body -->
            </div>
        </div>
    </div>
   
</section>
<div class="modal" id="progressmodal" data-keyboad="false" data-backdrop="static" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Update product Prices By Category</h4>
                <button class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body" id="progrdata">
                <div class="form-group">
                    <label id="pincdelb">Product Category</label>
                    @*" -Select- "*@

                    @Html.DropDownList("ProductCategoryId", null, new { @id = "categoryID" })
                    @Html.StarkDropDownAjaxLink("/ProductCategory/Create", "AddMore", "")
                   
                </div>
                <div class="form-group">
                    <label>Enter The Percentage change in price</label>
                    <input type="number" id="percnt" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn-primary" id="upbtn">Update</button>
                <button class="btn-primary" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>

<div class="modal" id="dprice" data-keyboad="false" data-backdrop="static" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Decrease product Prices By Category </h4>
                <button class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body" id="progrdata">
                <div class="form-group">
                    <label id="pincdelb">Product Category</label>
                    @*" -Select- "*@
                    @Html.DropDownList("ProductCategoryId", null, new { @id = "catID" })
                    @Html.StarkDropDownAjaxLink("/ProductCategory/Create", "AddMore", "")

                </div>
                <div class="form-group">
                    <label>Enter The Percentage change in price</label>
                    <input type="number" id="dcnt" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn-primary" id="dcbtn">Update</button>
                <button class="btn-primary" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>

<div class="modal" id="allprice" data-keyboad="false" data-backdrop="static" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Increase All  Prices  </h4>
                <button class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body" id="progrdata">
                @*<div class="form-group">
                    <label id="pincdelb">Product Category</label>
                   
                    @Html.DropDownList("ProductCategoryId", null, new { @id = "catID" })
                    @Html.StarkDropDownAjaxLink("/ProductCategory/Create", "AddMore", "")

                </div>*@
                <div class="form-group">
                    <label>Enter The Percentage change in price</label>
                    <input type="number" id="allnt" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn-primary" id="allbtn">Update</button>
                <button class="btn-primary" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>
<div class="modal" id="dallprice" data-keyboad="false" data-backdrop="static" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Decrease All  Prices  </h4>
                <button class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body" id="progrdata">
                @*<div class="form-group">
                        <label id="pincdelb">Product Category</label>

                        @Html.DropDownList("ProductCategoryId", null, new { @id = "catID" })
                        @Html.StarkDropDownAjaxLink("/ProductCategory/Create", "AddMore", "")

                    </div>*@
                <div class="form-group">
                    <label>Enter The Percentage change in price</label>
                    <input type="number" id="dallnt" />
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn-primary" id="dallbtn">Update</button>
                <button class="btn-primary" data-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>
<!-- Small boxes (Stat box) -->




<script type="text/javascript">

    $(document).ready(function () { 
      
       // load_quagga();
        //updatep();
        //decreasepricebycategory();
        //increasepricesofallproducts();
        //decreasepricesofallproducts();
        loadProducts();
    });
    let data = ''

    window.onload = function () {
        window.document.body.addEventListener('keydown', function (event) {
            if (event.keyCode == 13 || event.keyCode == 16 || event.keyCode == 17) {
                event.preventDefault();
                return;
            }

            if (event.ctrlKey) {
                event.preventDefault();
                return;
            }

            data += event.key
            console.log(data)
        });
    }
    //load products
    function loadProducts(){
        var oTableProduct = "";
        var ControlerNameProduct = "@Url.Content("~/Product")";
        // debugger;
        oTableProduct = $("#tbProduct").dataTable({
            
            "bProcessing": true,
            "bDestroy": true,
            "dom": 'lBfrtip',
            "sAjaxSource": "@Url.Content("~/Product/GetGrid")",
            "buttons": [
                 { extend: 'copyHtml5', exportOptions: { columns: ':visible' } }
                , { extend: 'excelHtml5', exportOptions: { columns: ':visible' } }
                , { extend: 'csvHtml5', exportOptions: { columns: ':visible' } }
                , { extend: 'pdfHtml5', exportOptions: { columns: ':visible' } }
                , { extend: 'print', exportOptions: { columns: ':visible' } }
                , 'colvis'
            ], columnDefs: [{ visible: false }],
            "pageLength": 10,
            "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]],
            "aoColumns": [

            {
                "mRender": function (oObj, type, row) {

                    var buttons = '<div class="text-center"> <div class="btn-group text-left"> <button type="button" class="btn btn-default btn-xs btn-warning dropdown-toggle" data-toggle="dropdown">Action <span class="caret"></span></button>';
                    buttons += '<ul class="dropdown-menu pull-left" role="menu">';
                    //buttons += '<li><a href="' + ControlerNameProduct + "/MultiViewIndex/" + row[0] + '"><i class="fa fa-edit"></i>Full Edit</a></li>';
                    buttons += '<li class="divider"></li>';
                    buttons += '<li><a href="' + ControlerNameProduct + "/Edit/" + row[0] + '"><i class="fa fa-pencil-square"></i>Edit</a></li>';
                    //buttons += '<li><a href="' + ControlerNameProduct + "/Edit/" + row[0] + '" data-ajax-update="#SkEdit" data-ajax-success="openModalDialog(\'SkEdit\', \'Edit\')" data-ajax-mode="replace" data-ajax-method="GET" data-ajax-failure="clearModalDialog(\'SkEdit\');alert(\'Ajax call failed\')" data-ajax-begin="prepareModalDialog(\'SkEdit\')" data-ajax="true"><i class="fa fa-pencil-square"></i>Quick Edit</a></li>';
                    buttons += '<li><a href="' + ControlerNameProduct + "/Details/" + row[0] + '" data-ajax-update="#SkDetails" data-ajax-success="openModalDialog(\'SkDetails\', \'Details\')" data-ajax-mode="replace" data-ajax-method="GET" data-ajax-failure="clearModalDialog(\'SkDetails\');alert(\'Ajax call failed\')" data-ajax-begin="prepareModalDialog(\'SkDetails\')" data-ajax="true"><i class="fa fa-info-circle"></i>Quick Details</a></li>';
                    buttons += '<li><a href="' + ControlerNameProduct + "/Delete/" + row[0] + '" data-ajax-update="#SkDelete" data-ajax-success="openModalDialog(\'SkDelete\', \'Delete\')" data-ajax-mode="replace" data-ajax-method="GET" data-ajax-failure="clearModalDialog(\'SkDelete\');alert(\'Ajax call failed\')" data-ajax-begin="prepareModalDialog(\'SkDelete\')" data-ajax="true"><i class="fa fa-trash-o"></i> Delete</a></a></li>';
                    buttons += '<li><a href="' + ControlerNameProduct + "/ProductHistory/" + row[0] + '"><i class="fa fa-credit-card"></i>Product History</a></li>';

                    buttons += '</ul>';
                    buttons += '</div></div>';
                    return buttons;
                }

            }, {}
, {}
, {
    "mRender": function (oObj, type, row) {
        return "<a href='/ProductCategory/' target='_blank' class='btn btn-tiny'  /> " + oObj + " </a>";
    }
}, {}
                , {}
                , {}, {}
, {}
//, {}
//, {
//    "mRender": function (oObj, type, row) {
//        if (oObj == null)
//            return "";
//        else
//            return "<img src='/Uploads/" + oObj + "'  style='width:100px;height:85px' />";
//    }
//                }
                //, {}
//, {}
//, {}
//, {}
              //  , {}
          //      , {}
         //       , {}
                , {}
, {}
]

        }
      );
       
}
    // update product prices using jquerry
    $(function updatep() {

        $("#upbtn").click(function () {


            var catId = $("#categoryID").val();
            var pernt = $("#percnt").val();

            $.ajax({
                type: "POST",
                //url: "~/UpdatePrice?Id=" + catId + "&addprice=" + pernt + "",
                url: "@Url.Action("UpdatePrice", "Product")?Id=" + catId + " &addprice= " + pernt + "",

                datatype: "json",
                data: '{Id:' + catId + ', addprice: "' + pernt + '"}',
                success: function () {

                    $("#upbtn").closest("tr").find("td:eq(4)").text(400);
                    $('#progressmodal').modal('hide');

                    loadProducts();
                },
                error: function (req, textStatus, errorThrown) {
                    //this is going to happen when you send something different from a 200 OK HTTP
                    alert('Ooops, something happened: ' + textStatus + ' ' + errorThrown);
                }

            });


        });
    });
    $(function decreasepricebycategory() {

        $("#dcbtn").click(function () {

            var catId = $("#catID").val();
            var pernt = $("#dcnt").val();

            $.ajax({
                type: "POST",

                url: "@Url.Action("DecreasePrice", "Product")?Id=" + catId + " &addprice= " + pernt + "",

                datatype: "json",
                data: '{Id:' + catId + ', addprice: "' + pernt + '"}',
                success: function () {
                    $("#upbtn").closest("tr").find("td:eq(4)").text(400);
                    $('#dprice').modal('hide');
                    loadProducts();

                },
                error: function (req, textStatus, errorThrown) {
                    //this is going to happen when you send something different from a 200 OK HTTP
                    alert('Ooops, something happened: ' + textStatus + ' ' + errorThrown);
                }

            });


        });
    });
    //update all products
    $(function increasepricesofallproducts() {

        $("#allbtn").click(function () {


            //var catId = $("#categoryID").val();
            var pernt = $("#allnt").val();

            $.ajax({
                type: "POST",
                //url: "~/UpdatePrice?Id=" + catId + "&addprice=" + pernt + "",
                url: "@Url.Action("increaseall", "Product")?addprice=" + pernt + "",

                datatype: "json",
                data: '{addprice: "' + pernt + '"}',
                success: function () {
                    $("#upbtn").closest("tr").find("td:eq(4)").text(400);
                    $('#allprice').modal('hide');
                    loadProducts();

                },
                error: function (req, textStatus, errorThrown) {
                    //this is going to happen when you send something different from a 200 OK HTTP
                    alert('Ooops, something happened: ' + textStatus + ' ' + errorThrown);
                }

            });


        });
    });
    //decrease all products
    $(function decreasepricesofallproducts() {

        $("#dallbtn").click(function () {


            //var catId = $("#categoryID").val();
            var pernt = $("#dallnt").val();

            $.ajax({
                type: "POST",
                //url: "~/UpdatePrice?Id=" + catId + "&addprice=" + pernt + "",
                url: "@Url.Action("decreaseall", "Product")?addprice=" + pernt + "",

                datatype: "json",
                data: '{addprice: "' + pernt + '"}',
                success: function () {
                    $("#upbtn").closest("tr").find("td:eq(4)").text(400);
                    $('#dallprice').modal('hide');
                    loadProducts();

                },
                error: function (req, textStatus, errorThrown) {
                    //this is going to happen when you send something different from a 200 OK HTTP
                    alert('Ooops, something happened: ' + textStatus + ' ' + errorThrown);
                }

            });


        });
    });
    //BarCode reader
    function order_by_occurrence(arr) {
        var counts = {};
        arr.forEach(function (value) {
            if (!counts[value]) {
                counts[value] = 0;
            }
            counts[value]++;
        });

        return Object.keys(counts).sort(function (curKey, nextKey) {
            return counts[curKey] < counts[nextKey];
        });
    }
    function load_quagga() {
        if ($('#barcode-scanner').length > 0 && navigator.mediaDevices && typeof navigator.mediaDevices.getUserMedia === 'function') {



            Quagga.init({
                inputStream: {
                    name: "Live",
                    type: "LiveStream",
                    numOfWorkers: navigator.hardwareConcurrency,
                    target: document.querySelector('#barcode-scanner')
                },
                decoder: {
                    readers: ['ean_reader', 'ean_8_reader', 'code_39_reader', 'code_39_vin_reader', 'codabar_reader', 'upc_reader', 'upc_e_reader']
                }
            }, function (err) {
                if (err) { console.log(err); return }
                Quagga.initialized = true;
                Quagga.start();
            });

            var last_result = [];
            if (Quagga.initialized == undefined) {
                Quagga.onDetected(function (result) {
                    var last_code = result.codeResult.code;
                    last_result.push(last_code);
                    if (last_result.length > 20) {
                        code = order_by_occurrence(last_result)[0];
                        last_result = [];
                       // Quagga.stop();

                        $("#tbProduct_filter input").val(last_code);
                        //$.ajax({
                        //    type: "POST",
                        //    url: '/products/get_barcode',
                        //    data: { upc: code }
                        //});
                    }
                });
            }

        }
    };
   
</script>

