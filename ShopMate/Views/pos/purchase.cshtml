@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section ScriptsOrCss
{
    <link href="~/Content/DataTable/new/dataTables.bootstrap.css" rel="stylesheet" type="text/css" />
    <script src="~/Content/DataTable/new/jquery.dataTables.min.js" type="text/javascript"></script>
    <link href="~/Content/DataTable/new/jquery.dataTables.min.css" rel="stylesheet" type="text/css" />

}
 
<style>
    #tbProduct_wrapper select
    {
       display:none;
    }
    #tbProduct_filter input
    {
        width:190px;
        margin-right:20px;
    }
    .selecttax {
        font-size: 11px;
        height: 20px;
        padding: 0;
        width: 150px;
    }
</style>

<script type="text/javascript">

    $(document).on("keypress", function (e) {
        if (e.which == 13) {
            document.getElementById("btnPlus").click(); 
        }
    });
    let data = ''

    window.onload = function () {
        window.document.body.addEventListener('keydown', function (event) {
            if (event.keyCode == 13 || event.keyCode == 16 || event.keyCode == 17) {
                event.preventDefault();
                return;
            }

            if (event.ctrlKey) {
                event.preventDefault();
                return;
            }

            data += event.key
            console.log(data)
        });
    }
    $(document).ready(function () {
        var oTableProduct = "";
        var ControlerNameProduct = "@Url.Content("~/pos")";
        // debugger;
        oTableProduct = $("#tbProduct").dataTable({
            "bRetrieve": true,
            "bProcessing": true,
            "dom": 'lBfrtip',
            "sAjaxSource": "@Url.Content("~/pos/GetProductPurchase")",
            "pageLength": 50,
            "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]],
            "aoColumns": [
                 {
                     "mRender": function (oObj, type, row) {

                         var content = '<img src="/Uploads/' + row[3] + '" style="width:50px;height:50px;" /> &nbsp;<b>' + row[6] + '</b>&nbsp;' + row[0] + ' <br/><b>Price:' + row[1] + ' </b>';

                         return content;
                     }

                 }, 
            {
                "mRender": function (oObj, type, row) {

                    var buttons = '<div class="text-center"> <input type="number" value="1" style="width:44px;" id="Qty' + row[2] + '"/> <button onclick="addInv(' + row[2] + ',' + row[4] + ',' + row[5] + ',\'' + row[0] + '\')" type="button" id="btnPlus" class="btn btn-default btn-xs btn-warning" title="Click To Add" > <i class="fa fa-plus"></i></button></div>';

                    return buttons;
                }

            }
            ]

        });

        $(".dataTables_filter input").on("keypress", function (e) { 
            if (e.which == 13) {
                document.getElementById("btnPlus").click(); 
            }
        });

        $("#tbody").on('click', ".delete", function () {
            var amt = $(this).attr("value"); 
            $(this).parents('.item-row').remove();
            GrossAmount(-amt);
        });


        $("#btnPayOut").on('click', function () {
            if (confirm("are you sure you want to payout!") == true) {
                var products = [];
                //var quantity = [];

                $("tr.item-row").each(function () {
                    $this = $(this);
                    products.push({ product: $this.attr("id"), qty: $this.attr("qty") });
                    //quantity.push($this.attr("qty")); 
                });

                
                if ($("#VendorUserId").val() > 0) {
                    $.ajax({
                        url: "/pos/newPurchase",
                        type: "Post",
                        data: { 'products': products, 'VendorUserId': $("#VendorUserId").val(), 'PaymentModeId': $("#PaymentModeId").val(), 'PurchaseNote': $("#PurchaseNote").val(), 'TaxName': $(".selecttax").val() },
                        dataType: 'json',//json
                        success: function (data) {
                            if (data[0].msg == "Done") {
                                $.sticky('<br/> Action Successfully Executed', { stickyClass: 'success' });
                                window.location = "@Env.GetSiteRoot()/pos/print/" + data[0].value;
                            }
                            else {
                                $.sticky('<br/> ! Something  went wrong. <br/> ' + data + '', { stickyClass: 'error' });
                            }
                        }, error: function () { $.sticky('<br/> ! Something  went wrong. <br/> ' + data + '', { stickyClass: 'error' }); }
                    });
                }
                else {
                    $.sticky('<br/> ! Select Vendor First. <br/>  ', { stickyClass: 'error' });
                }
                 

            }


        });

    });

    function addInv(id,price,tax,name)
    { 
        var qty = document.getElementById("Qty" + id).value;
       
        if (parseInt(qty) > 0)
        {
            if ($("#tbody").find("#" + id).length > 0) { //if row already have then remove old one
               $($("#tbody").find("#" + id).find(".delete")).click();
            }

            var total = (parseFloat(qty) * parseFloat(price));
            var TaxAmount = (parseFloat(tax) / 100) * (parseFloat(total));
            var grosstotal = total;
                //(total + TaxAmount);
            
           

            var div = '<tr class="item-row" id="' + id + '"  qty="' + qty + '" >   <td> <a class="delete" value="' + grosstotal + '" href="javascript:;" title="Remove row">X</a> &nbsp; ' + name + '</td> <td>' + price +' </td> <td> <select class="selecttax"><option value="CGST And SGST @@' + tax + '%">VAT @@' + tax + '%</option><option value="IGST @@' + tax + '%">NONE @@' + tax + '%</option> @Html.Raw(ViewBag.moreTax)  </select> </td>  <td>' + qty + '</td> <td>' + grosstotal + '</td>   </tr>';

            $("#tbody").append(div);
            GrossAmount(grosstotal);

            $("#divPayout").show();
        }
        else
        {
            alert("Incrise Quantity First");
        }
    }

    function GrossAmount(amount)
    {
        var oldAmount = $("#GrossAmount").text();
        var total = parseFloat(oldAmount) + parseFloat(amount);
        $("#GrossAmount").text(total.toFixed(2));
    //}
    //function GetCurrencyValue(GrossAmount, )
    //function UsdAmount(amount)
    //{
    //    var UsdAmount = amount * 2;
    }
    

</script>

<section class="content" style="background-color: #ff6a00; background-image: url(@Url.Content("~/Content/bg/brick-wall-dark.png"));">
    <!-- Small boxes (Stat box)  -->
    <div class="row">
        <div class="col-xs-4" style="padding-right:0px;">

            <section class="content">
                
                <div class="col-xs-12" style="padding-left:0px;height:640px;overflow:auto;">
                    
                    <div class="clearfix"></div>
                    <br />

                    <table id="tbProduct" class="display">
                        <thead>
                            <tr>
                                <th style="width:150px !important;"> Product  </th>
                                <th style="width:100px !important;"> Qty  </th>

                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>

                    <div class="clearfix"></div>
                    <br />

                </div>
                <div class="clearfix"></div>
                <br />
            </section>

        </div>

        <div class="col-xs-8" style="padding-left:0px;padding-right:0px;">
            <a href="~/Purchase/" class="btn btn-block btn-default btn-xs">Back To Purchase List</a>
            <div id="dvContainer" class="content invoice" style="padding: inherit; min-height: 135mm;  width:93%; ">

                <div style="margin-bottom:50px;">

                </div>
                <div class="row" style="font-size:12px;text-align:center;font-weight:bold;font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;">


                    <div style="text-align:center;font-size:30px;font-weight:bold;"> New Purchase</div>


                    <div style="text-align:left;margin-left:40px;">

                        <div style="height:10px; margin-bottom:10px; ">

                        </div>

                        <div class="row">

                            <div class="col-xs-12">

                                <div style="width:96%;">
                                    <table id="tbInvoiceItem" class="table table-striped table-bordered">
                                        <thead style="background-color:#e5e2e2;">
                                            <tr>

                                                <th>Product Name </th>
                                                <th>Price </th>
                                                <th>Tax </th>
                                                <th>Quantity </th>
                                                <th> Total </th>
                                            </tr>
                                        </thead>
                                        <tbody id="tbody"></tbody>
                                        <tfoot style="background-color:#e5e2e2;">
                                            <tr>
                                                <th> </th>
                                                <th> </th>
                                                <th></th>
                                                <th> </th>
                                                <th> </th>
                                            </tr>

                                            <tr>
                                                <th> </th>
                                                <th> </th>
                                                <th></th>
                                                <th>Gross Amount </th>
                                                <th id="GrossAmount">0</th>
                                            </tr>
                                        </tfoot>
                                    </table>

                                    <div id="divPayout" style="display:none;">
                                        @Html.DropDownList("VendorUserId", " -Select Vendor- ")<br />
                                        @Html.DropDownList("PaymentModeId", " -Select Payment Mode- ")<br />
                                        <input type="text" id="PurchaseNote" placeholder="Any Remarks" value="Purchase" style="width:325px" />
                                        <input style="margin-top:10px;" type="button" id="btnPayOut" class="btn btn-primary" value="PayOut And Print" />
                                    </div>
                                </div>


                            </div>


                        </div>
                        <div style="height:10px; margin-bottom:20px;">

                        </div>



                    </div>


                    <div class="clearfix"></div>


                </div>


                <div class="row">
                    <div class="col-xs-12">
                        <div class="col-xs-5" style="padding-left:60px;"> @DateTime.Now.ToString("dd/MMM/yyyy hh:mm:tt")</div>

                    </div>
                </div>
            </div>
        </div>
    </div>


</section>

