@model ShopMate.Models.InvoiceFormat

@{
    ViewBag.Title = "Are you sure you want to delete";
    if (Ajax.IsThisAjaxRequest())
    {
        Layout = null;
    }
}


@Html.StarkAjaxFormSubmiter("frmInvoiceFormat", "tbInvoiceFormat", true, "Action Successfully Executed")

@{
    if (!Ajax.IsThisAjaxRequest())
    {
        <section class="content-header">
            <h1>
                @ViewBag.Title
                <small>@ViewBag.Title</small>
            </h1>
        </section>
    }
}

<section class="content">
    <!-- Small boxes (Stat box) -->
    <div class="row">
        <div class="col-xs-12">

            <div class="box box-solid box-primary">
                <div class="box-header">
                    <h3 class="box-title">Detail of InvoiceFormat</h3>
                </div><!-- /.box-header -->
                <div class="box-body table-responsive">
                  
                    <div class="widget-sec">
                        <div class="streaming-table">
                            <table class="table table-striped table-bordered">

                                <tr>
                                    <td>CompanyName</td>
                                    <td>@Html.DisplayFor(model => model.CompanyName)</td>
                                </tr>
                                <tr>
                                    <td> &nbsp;</td>
                                    <td> <img src="~/Uploads/@Model.Logo" width="200" /> </td>
                                </tr>
                                <tr>
                                    <td>AddressInfo</td>
                                    <td>@Html.DisplayFor(model => model.AddressInfo)</td>
                                </tr>
                                <tr>
                                    <td>OtherInfo</td>
                                    <td>@Html.DisplayFor(model => model.OtherInfo)</td>
                                </tr>
                                <tr>
                                    <td>FooterInfo</td>
                                    <td>@Html.DisplayFor(model => model.FooterInfo)</td>
                                </tr>
                                <tr>
                                    <td>WarehouseId</td>
                                    <td>@Html.DisplayFor(model => model.WarehouseId)</td>
                                </tr>


                            </table>
                            <div class="box-footer">

                                  @using (Html.BeginForm("Delete", "InvoiceFormat", FormMethod.Post, new {  id = "frmInvoiceFormat" }))
                                {
                                    @Html.AntiForgeryToken()

                                    @Html.StarkInputButtion("Delete","submit","btn btn-danger","")
                                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



